---
title: "lab1"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

# Задание 1

## 1
>Сгенерируйте 200 случайных значений из стандартного экспоненциального распределения и сохраните в вектор
_exp.1_. 

```{r}
exp.1 <- rexp(n = 200)
```

>Найдите среднее и стандартное отклонение из данных этого вектора.

```{r}
mean(exp.1)
sd(exp.1)
```

## 2
>Повторите, использовав следующие значения параметра $\lambda$: 0.1, 0.5, 5, 10, и сохраните результаты в векторы: _exp.0.1_, _exp.0.5_, _exp.5_, _exp.10_.

```{r}
exp.0.1 <- rexp(n = 200, rate = 0.1)
exp.0.5 <- rexp(n = 200, rate = 0.5)
exp.5 <- rexp(n = 200, rate = 5)
exp.10 <- rexp(n = 200, rate = 10)
```

## 3
>Функция ``plot()`` общая функция для визуализации данных. Функция ``hist()`` принимает данные и раскладывает их по корзинам. Чтобы использовать эти функции нужно для начала передать им данные, которые хотим визуализировать.

## 4
>Используйте функцию ``hist()`` для построения гистограммы полученного ранее стандартного распределения.

```{r}
hist(exp.1)
```

## 5
>Используйте функцию ``plot()`` с тем же самым вектором для построения графика значений в порядке следования.

```{r}
plot(exp.1)
```

## 6
>Используйте функцию ``plot()`` с любыми двумя полученными ранее векторами, чтобы получить диаграмму рассеяния ([scatterplot](https://www.mathsisfun.com/data/scatter-xy-plots.html)) этих векторов относительно друг друга.

```{r}
plot(exp.0.5, exp.5)
```

## 7
>Теперь сравним свойства всех наших векторов. Для начала создадим вектор,
содержащий средние значения каждого из 5 полученных ранее распределений в порядке их создания.

```{r}
exp.arguments <- c(1., 0.1, 0.5, 5., 10.)
```

>Полученный вектор сохраним в переменную _exp.means_. 

```{r}
exp.means <- c(mean(exp.1), mean(exp.0.1), mean(exp.0.5), mean(exp.5), mean(exp.10))
```

>Далее необходимо создать следующие диаграммы рассеяния:

```{r}
exp.sds <- c(sd(exp.1), sd(exp.0.1), sd(exp.0.5), sd(exp.5), sd(exp.10))
```

## 8
>Вектор с пятью средними относительно каждого из пяти векторов с различными значениями $\lambda$.

```{r}
plot(exp.means, exp.arguments, type = "p")
```

Чем больше значение $\lambda$, тем меньше среднее отклонение.

## 9
>Вектор с пятью стандартными отклонениями относительно каждого из пяти векторов с различными значениями $\lambda$.

```{r}
plot(exp.sds, exp.arguments, type = "p")
```

Чем больше значение $\lambda$, тем меньше стандартное отклонение.

## 10
>Вектор с пятью средними относительно вектора с пятью стандартными отклонениями.

```{r}
plot(exp.sds, exp.means, type = "p")
```

Среднее отклонение и стандартное отклонение почти идентичны.

# Задание 2

## 1
>Для демонстрации сгенерируйте 1100000 (1 млн. 100 тысяч) чисел из стандартного экспоненциального распределения и сохраните их в вектор _huge.exp.1_.

```{r}
huge.exp.1 <- rexp(n = 1100000)
```

> Получите среднее и стандартное отклонение.

```{r}
mean(huge.exp.1)
sd(huge.exp.1)
```

## 2
>Получите гистограмму _huge.exp.1_. 

```{r}
hist(huge.exp.1)
```

>Она соответствует функции ``1 - e:^(-x)``? Должна ли?

Гистограмма соответствует функции ``1 - e:^(-x)``. Должна, так как _huge.exp.1_ получен функцией для задания стандартного экспоненциального отклонения.

## 3
>Получите среднее всех чисел из _huge.exp.1_ строго больших 1. Для проверки выборки может потребоваться новый вектор.

```{r}
mean(huge.exp.1[] > 1)
```

## 4
>Получите матрицу _huge.exp.1.mat_, содержащую 1100 строк и 1000 колонок. Постройте гистограмму, использовав полученную матрицу. 

```{r}
huge.exp.1.mat <- matrix(huge.exp.1, 1100, 1000)
hist(huge.exp.1.mat)
```

>Что можно сказать о полученных данных?

Это гистограмма экспоненциального распределения.

## 5
>Рассчитайте среднее 137-й колонки из _huge.exp.1.mat_.

```{r}
mean(huge.exp.1.mat[,137])
```

## 6
>Найдите среднее всей 1000 колонок и сохраните их в вектор. Отобразите диаграмму средних значений колонок. 

```{r}
huge.exp.1.mat.colMeans <- colMeans(huge.exp.1.mat)
barplot(huge.exp.1.mat.colMeans)
```

>Объясните почему форма диаграммы отличается от виденных ранее.

Форма диаграммы отличается, потому что мы рассматриваем только средние значения.

## 7
>Рассчитайте квадрат каждого значения из _huge.exp.1_ и найдите среднее и стандартное отклонение полученного вектора квадратов.

```{r}
mean(huge.exp.1 ^ 2)
sd(huge.exp.1 ^ 2)
```

>Объясните отличие от соответствующих показателей вектора _huge.exp.1_. Вам может помочь формула, по которой R рассчитывает стандартное отклонение: ``sqrt(sum((x - mean(x))^2) / (n - 1))``.

Среднее и стандартное отклонения увеличиваются с повышением значения вектора. Стандартное отклонение увеличивается быстрее из-за квадрата в формуле, по которой оно рассчитывается.
